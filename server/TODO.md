## Auth
- [ ] I have to ensure that the user is authenticated from the web app before that the API KEY (CLI login) is generated.
I just have to check if the access token exist in the db.
- [ ] Add functionality to revoke API KEYs (take in account that showed field in db have to be reset).
